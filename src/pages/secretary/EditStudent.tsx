// we can edit student, change mark, edit subjects and factors
import React, { useState } from 'react';
const EditStudent: React.FC = () => {
    const [subjectsRow, setSubjectsRows] = useState([]);

    return (
        <div>
            
        </div>
    );
}